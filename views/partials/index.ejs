<%include ./partials/header%>
    <h1>This is the index page</h1>
    
    <% if(!currentUser){ %>
        <h1>Hellloooo</h1>
    <% } else { %>
    
        <p>You have been logged in</p>
        <a href="/user/<%=currentUser._id%>">See the id</a>
    <% } %>

<%include ./partials/footer%>